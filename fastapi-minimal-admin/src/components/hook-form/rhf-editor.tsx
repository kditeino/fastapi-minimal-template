// Editor component removed - stub to prevent import errors
export type RHFEditorProps = { name: string; helperText?: string; [key: string]: unknown };

export function RHFEditor({ name, helperText, ...other }: RHFEditorProps) {
  return null;
}
