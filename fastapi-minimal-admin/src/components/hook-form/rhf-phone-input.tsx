// PhoneInput component removed - stub to prevent import errors
export type RHFPhoneInputProps = { name: string; helperText?: string; [key: string]: unknown };

export function RHFPhoneInput({ name, helperText, ...other }: RHFPhoneInputProps) {
  return null;
}
