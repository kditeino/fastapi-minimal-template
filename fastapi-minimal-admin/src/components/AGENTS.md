<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-04-09 | Updated: 2026-04-09 -->

# components

## Purpose
Shared, feature-agnostic UI components. Identical structure to `next-ts/src/components/` — refer to that AGENTS.md for the full component catalog.

## For AI Agents

### Working In This Directory
- Same component set and patterns as `next-ts/src/components/`.
- Each component has its own directory with `index.ts` barrel export.
- Import via: `import { Label } from 'src/components/label'`.
- Changes here should be mirrored in `next-ts/src/components/` (and vice versa) unless the difference is framework-specific.

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->
